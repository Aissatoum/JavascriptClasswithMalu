//Create a function that checks an array of numbers and checks if they are an integer or not

//[1, 1.5,2,2.5,3,3.5]

//create a function 
//declare a variable 
//compare 
  var num = [1, 1.5,2,2.5,3,3.5];
function check(){
if(num % 2 == 0){
    console.log(`${num} is an interger`);

}else{
    console.log(`${num} is not an interger`);
}
}